# 1) con il codice seguente, crea le due matrici casualiset.seed(1234)mat4 <- matrix(sample(1:10, 4*5, replace=TRUE), nrow=4, ncol=5)mat5 <- matrix(sample(1:10, 4*5, replace=TRUE), nrow=4, ncol=5)# 2) Scopri il numero di colonne e di righe delle due matrici ncol(mat4)ncol(mat5)nrow(mat4)nrow(mat5)# 3) trasponi la matrice mat4t(mat4)# 4) Calcola la media e la somma delle righe e delle colonne della matrice mat4rowMeans(mat4)colMeans(mat4)colSums(mat4)rowSums(mat4)# 5) Seleziona da mat5 la terza colonnamat5[, 3]# 6) Seleziona ora tutta la seconda rigamat5[2, ]# 7) Seleziona le righe dalla seconda alla quarta esclusamat5[2:3,]# 8) Attribuisci i nomi col1, col2 e col3 eccetera alle colonne della matrice mat5# Attribuisci anche i nomi delle righe: row1, row2 eccetera. # Se è necessario,  verifica nuovamente il numero di righe e colonne# ristampa la matrice per verificare che i nomi siano corretticolnames(mat5) <- c("col1", "col2", "col3", "col4", "col5")rownames(mat5) <- c("row1", "row2", "row3", 'row4')print(mat5)# 9) verifica se è possibile sommare e moltiplicare le due matricimat4 + mat5mat4 * mat5# 10) sostituisci in mat4 tutti gli elementi maggiori di 5 con 0mat4[mat4 > 5] <- 0